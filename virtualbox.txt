//
// **********************************************************
// * UNLV						    *
// *							    *
// * virtualbox settings  guide				    *
// **********************************************************
//
//
///
///

  // virtualbox settings
  // https://www.virtualbox.org
  // https://learn.microsoft.com/en-US/cpp/windows/latest-supported-vc-redist?view=msvc-170
  //
    	// ubuntu setting
	select "Skip Unattended Installation"
	
	// endeavouros setting
	set "Linux" for Type
	set "Other Linux (64-bit)" for Version
	
  // create virtual machine
  set virtual hard disk to 80.00 gb
  
  // settings
  set "Bidirectional" for Shared Clipboard in Advanced		//General
  set "Bidirectional" for Drag'n'Drop in Advanced		//General
  set half of full memory for Base Memory in Motherboard	//System
  set maximum in green for Processor (s) in Processor		//System
  set maximum for Video Memory in Screen			//Display
  select "Empty" in Storage Devices				//Storage
	 "Choose a disk file..." in Attributes

  	// Shared Folders
  		// virtualbox settings
  		click "Adds new shared folder."
  		set windows folder path
  		set "Auto-mount"
	
  // option of virtualbox 6.1 settings
  set "KVM" for Paravirtualization Interface			//System
      "Hyper-V" for windows
  set "Enable 3D Acceleration"					//Display
  
  // option of windows 11 settings
  	// disable windows hypervisor
 	open "Command Prompt" as administrator
  		$ bcdedit /set hypervisorlaunchtype off		//disable
						    auto	//enable
						    
  	// disable memory integrity
  	find "Settings" in search on task bar
  	go to "Privacy & security" and "Windows Security"
  	go to "Device security" and "Core isolation details"
  	disable "Memory integrity"  
  	reboot
	
  // option of linux commands
  	$ groups			//print group names
  	$ sudo su -l user{$username}	//substitute user id
  	$ groups			//confirm "vboxsf"
  	$ ls -lh			//list information

//....oooOO0OOooo........oooOO0OOooo........oooOO0OOooo........oooOO0OOooo......
